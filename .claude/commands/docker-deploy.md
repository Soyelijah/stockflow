# /docker-deploy

Professional containerization and deployment strategies. Covers Docker best practices, orchestration, CI/CD pipelines, and Kubernetes fundamentals.

## Instructions

Read the skill file at `.claude/skills/docker-deploy/SKILL.md` and follow its instructions to complete the user's request.

If the skill has reference files in `.claude/skills/docker-deploy/references/`, read those too for deeper context.

If the skill has scripts in `.claude/skills/docker-deploy/scripts/`, use them when applicable.
